<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Peliculas!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
            <link rel="stylesheet" href="{{ asset('css/stylesheet.css') }}">
        {% endblock %}

    </head>
    <body>
        {% block body %}
            <nav class="navbar navbar-expand-lg navbar-light">
{#                <a class="" href="{{ path('dashboard') }}"><img src="{{ asset("img/logo.png")}}" width="50%" style="padding: 0px" alt=""></a>#}
                <a  class="navbar-brand" href="{{ path('dashboard') }}" ><img src="{{ asset("img/logo.png")}}" alt=""></a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav mr-auto">
{#                        <li class="nav-item">#}
{#                            <a class="nav-link" href="{{ path('VerPeliculas') }}">Para ti</a>#}
{#                        </li>#}
                        <li class="nav-item active">
                            <a class="nav-link" href="{{ path('RegistrarPeliculas') }}">Crear Publicacion</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('MisPeliculas') }}">Mis Peliculas</a>
                        </li>
                    </ul>
                    <form class="form-inline my-2 my-lg-0">
                        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                        <button class="btn btn-success my-2 my-sm-0" type="submit">Search</button>
                    </form>

                        <a class="nav-link" href="{{ path('app_logout') }}">Cambiar Usuario</a>

                </div>
            </nav>
        {% endblock %}
        {% block javascripts %}
            <script src="{{ asset('js/jquery.min.js') }}"></script>
            <script src="{{ asset('js/bootstrap.min.js') }}"></script>
{#            <script src="{{ asset('bundles/fosjsrouting/js/router.min.js') }}"></script>#}
{#            <script src="{{ path('fos_js_routing_js', { callback: 'fos.Router.setData' }) }}"></script>#}
{#            <script src="{{ asset('js/ajax-call.js') }}"></script>#}
        {% endblock %}
    </body>
</html>
